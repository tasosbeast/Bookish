# Required during production builds. Include /api; use the public HTTPS API URL.
# Production frontend and backend must share the same site for refresh cookies.
VITE_API_BASE_URL=http://localhost:3000/api
